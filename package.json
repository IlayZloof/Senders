{
  "name": "senders",
  "version": "2.0.0",
  "description": "Senders — ספוטי קפיצה בישראל",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "mkdir -p .wrangler-tmp && TMPDIR=\"$PWD/.wrangler-tmp\" wrangler deploy --config wrangler.toml --no-autoconfig"
  },
  "devDependencies": {
    "vite": "^6.3.5",
    "wrangler": "^4.123.0"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.110.2",
    "leaflet": "^1.9.4"
  }
}
